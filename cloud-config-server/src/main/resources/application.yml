server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeevan-gowda/Micro-Services/tree/main/config-server-properties
          clone-on-start: true
